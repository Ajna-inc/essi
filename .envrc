export DYLD_LIBRARY_PATH="$(pwd)/askar-go/native:${DYLD_LIBRARY_PATH}"

# Optional: keep OpenSSL on PATH if needed by transitive deps
export PKG_CONFIG_PATH="/opt/homebrew/opt/openssl@3/lib/pkgconfig:${PKG_CONFIG_PATH}"


